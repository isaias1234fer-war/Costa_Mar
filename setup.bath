@echo off
echo Instalando Sistema Restaurante...
python -m venv venv
call venv\Scripts\activate
pip install -r requirements.txt
echo Instalacion completada!
echo Ejecutar con: streamlit run app.py --server.port 8502
pause